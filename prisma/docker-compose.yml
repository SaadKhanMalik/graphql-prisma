version: '3'
services:
  prisma:
    image: prismagraphql/prisma:1.34
    restart: always
    ports:
    - "4466:4466"
    environment:
      PRISMA_CONFIG: |
        port: 4466
        # uncomment the next line and provide the env var PRISMA_MANAGEMENT_API_SECRET=my-secret to activate cluster security
        # managementApiSecret: my-secret
        databases:
          default:
            connector: postgres
            host: ec2-174-129-227-146.compute-1.amazonaws.com
            database: dfmhjmcstim89v
            schema: prisma
            user: pyrqixfvsacxsw
            password: a57990c0d708d30dde0273c750b8b283c664323e572db51212eb20f15a047d8f
            ssl: true
            rawAccess: true
            port: '5432'
            migrations: true